<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Welcome to Rock Paper Scissors Lizard Spock!</h1>
    <h1> <%= `${player1.name}` %> chose: <%= `${player1.choice}` %> </h1>
    <h1> <%= `${player2.name}` %> chose: <%= `${player2.choice}` %> </h1>
    <h1> The winner is : <b><%= `${winner}` %></b></h1>
    <a id="turn-link"href="/"><h2>Play again</h2></a>
  </body>
</html>
